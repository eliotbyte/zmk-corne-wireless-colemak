/* Add wakeup-source to kscan — missing in upstream charybdis module.
 * Without this, the nRF52840 doesn't register the key matrix GPIO as a
 * wakeup source, causing unreliable BLE reconnect after deep sleep.
 */
&kscan0 {
    wakeup-source;
};

