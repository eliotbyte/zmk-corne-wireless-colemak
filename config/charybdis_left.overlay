/* Intentionally empty.
 *
 * The trackball hardware/input listener is defined on the side that has the
 * device (right), and processing/layer toggling is configured on the central
 * (dongle).
 */

